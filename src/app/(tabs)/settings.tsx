import { SafeAreaView, View, Text } from "react-native";

export default function Settings() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-bold text-2xl">Settings</Text>
    </View>
  );
}
